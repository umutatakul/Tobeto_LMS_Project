name: tobeto_lms_project
description: A new Flutter project.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ">=3.1.4 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  # Telefon hafızasındaki SVG dosyasını  image oalrak kullanmak için yüklendi
  flutter_svg: ^2.0.9
  google_fonts: ^6.1.0
  font_awesome_flutter: ^10.6.0
  flutter_rating_bar: ^4.0.1
  # animasyonlu arkaplan için kullanıldı
  animated_background: ^2.0.0
  #
  pie_chart: ^5.4.0
  #Datetime için kullanıldı
  intl: ^0.19.0
  flutter_heatmap_calendar: ^1.0.5
  #Eğitim hayatı ve deneyimler kısmı için olan uç uca çizgiler için kullanıldı
  timeline_tile: ^2.0.0
  #değerlendirmeler ksımıdna test yüzdeleri için kullanıldı
  # ayrıca my education carddaki prgress bar için
  percent_indicator: ^4.2.3
  cloud_firestore: ^4.15.4
  firebase_core: ^2.25.5
  firebase_auth: ^4.16.0
  flutter_bloc: ^8.1.3
  firebase_storage: ^11.6.5
  #fotorğrafları seçebiliriz
  image_picker: ^1.0.7
  #Certificates Container içinde dash line için
  #dotted_decoration: ^2.0.0
  #İnstagram gibi  gradient renkli olan ikonları kullanmak için paket buldum
  #gradient_icon: ^2.0.9
  #SVG dosyalarını istnereneten çekemk için kullanıyoruz
  flutter_svg_provider: ^1.0.7
  #home screendeki tabbarın dinamik olarak boyutlandırılması için kullnadık
  autoscale_tabbarview: ^1.0.2
  #video işlemleri için kullandığım kütüphaneler
  video_player: ^2.8.2
  visibility_detector: ^0.4.0+2
  flick_video_player: ^0.7.0
  # Sınava ait jotform sayfasını göstermek için kullandık
  webview_flutter: ^4.7.0
  # Google maps denemesi
  google_maps_flutter: ^2.5.3
  # Takvim için kütüphane

  flutter_neat_and_clean_calendar: ^0.3.0+12
dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^2.0.0
  #3.0.1 versiyonu var ama super tanımalamlarıma yüz tane hata verdi geri aldım

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/loginBackgroundLight.png
    - assets/images/loginBackgroundDark.png
    - assets/svgs/tobeto_named_logo.svg
    - assets/images/empty_survey_home.png
    # Anasayfa > Eğitimlerim için olan resimler

    - assets/images/course_images/ecmel_ayran.png
    - assets/images/course_images/istanbul_kod.png
    - assets/images/course_images/kodlama.png
    - assets/images/course_images/hosgeldin.png
    - assets/images/course_images/proje_asamalari.png
    - assets/images/course_images/mentor.png
    - assets/images/course_images/mobil_1B.png
    - assets/images/course_images/mobil_ogrenme.png
    - assets/images/course_images/softskill.png
    #   - images/a_dot_burr.jpeg
    #   - images/a_dot_ham.jpeg

    # Category pictures
    - assets/images/categoryHead.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # fonts:
  #   - family: ComicNeue
  #     fonts:
  #       - asset: packages/google_fonts/fonts/ComicNeue-Regular.ttf
  #       - asset: packages/google_fonts/fonts/ComicNeue-Bold.ttf
  #   - family: Tinos
  #     fonts:
  #       - asset: packages/google_fonts/fonts/Tinos-Regular.ttf
  #       - asset: packages/google_fonts/fonts/Tinos-Bold.ttf
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
