import 'package:flutter/material.dart';

class ColorsOfReviewScreen {
  Color firstContainerBackgroundColor(BuildContext context) =>
      Theme.of(context).colorScheme.secondary;
  Color containertextColor(BuildContext context) =>
      Theme.of(context).colorScheme.onSecondary;
}
